dev9DemoApp
    .controller('MainCtrl',
               ['$scope', '$stateParams',
        function($scope,   $stateParams)
        {
            // I don't do anything interesting
            $scope.demoValue = "Hello World!";
        }]);